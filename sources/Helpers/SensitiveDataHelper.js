const sensitiveDataHelper = {}

/**
 * content of that file contains sensitive data, so that file will be added in gitignore.
 */

const mailCredits = {
    /**
     * Empty body. Sensitive data here, will not be pushed...
     */
}

module.exports = sensitiveDataHelper;